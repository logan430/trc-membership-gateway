---
phase: 21-documentation-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
autonomous: true

must_haves:
  truths:
    - "New developer can understand project purpose in 30 seconds"
    - "New developer can run the app with 5 commands or less"
    - "All 37 environment variables are documented with sources"
  artifacts:
    - path: "README.md"
      provides: "Project overview and quick start"
      min_lines: 100
      contains: "Quick Start"
    - path: ".env.example"
      provides: "Complete environment template"
      min_lines: 80
      contains: "RECONCILIATION_AUTO_FIX"
  key_links:
    - from: "README.md"
      to: ".planning/ENVIRONMENT-SETUP.md"
      via: "documentation link"
      pattern: "ENVIRONMENT-SETUP"
    - from: "README.md"
      to: "docs/API.md"
      via: "documentation link"
      pattern: "API.md"
---

<objective>
Create README.md and complete .env.example with all environment variables

Purpose: Enable new developers to understand the project and configure it without hunting through source code
Output: README.md in project root, updated .env.example with all 37 variables
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-documentation-audit/21-RESEARCH.md
@src/config/env.ts
@.env.example
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>
  <action>
Create README.md in project root with:

1. **Header section:**
   - Project title: "The Revenue Council - Membership Gateway"
   - One-line description from PROJECT.md core value
   - Tech stack badges (Node.js, TypeScript, Express, Prisma, Discord.js, Stripe)

2. **Overview section:**
   - Brief explanation from PROJECT.md context
   - Target audience (solo developer or small team inheriting project)

3. **Technology Stack section:**
   - Node.js 20+ / TypeScript / Express 5.x
   - Prisma 7 + Supabase PostgreSQL
   - Discord.js 14 (bot operations)
   - Stripe (payments, webhooks)
   - Resend (optional, for production email)

4. **Quick Start section (5 commands max):**
   ```bash
   npm install
   # Configure .env (see below)
   npx prisma db push
   npx prisma db seed
   npm run dev
   ```

5. **Environment Configuration section:**
   - Brief overview of required vs optional vars
   - Reference to .env.example
   - Link to detailed setup: .planning/ENVIRONMENT-SETUP.md

6. **Available Scripts section:**
   - npm run dev (development with watch)
   - npm run build (TypeScript compilation)
   - npm start (production)
   - npm run lint (ESLint)
   - npx prisma studio (database GUI)

7. **Project Structure section:**
   ```
   src/
     config/     # Environment and constants
     routes/     # Express route handlers
     services/   # Business logic (Discord, Stripe, email)
     lib/        # Utilities (auth, validation)
     types/      # TypeScript type definitions
   prisma/       # Schema and migrations
   public/       # Static HTML pages
   .planning/    # Development documentation
   ```

8. **Documentation Links section:**
   - API Reference: docs/API.md
   - Deployment Guide: docs/DEPLOYMENT.md
   - Setup Guide: .planning/ENVIRONMENT-SETUP.md
   - Manual Testing: .planning/MANUAL-TESTING-GUIDE.md

9. **License section:**
   - Proprietary / All Rights Reserved (unless user specifies otherwise)

Keep tone professional but approachable. No emojis. Use clear section headers.
  </action>
  <verify>
    - File exists at README.md
    - Contains "Quick Start" section
    - Contains all 5 required commands
    - Links to ENVIRONMENT-SETUP.md
    - Links to docs/API.md
  </verify>
  <done>README.md exists with project overview, quick start, and links to detailed docs</done>
</task>

<task type="auto">
  <name>Task 2: Complete .env.example with all variables</name>
  <files>.env.example</files>
  <action>
Update .env.example to include ALL 37 variables from src/config/env.ts.

**Variables currently missing (add these):**
```env
# Email sender configuration
EMAIL_FROM_ADDRESS=The Revenue Council <noreply@revenuecouncil.com>
EMAIL_REPLY_TO=support@revenuecouncil.com

# Reconciliation (runs daily to sync Stripe/Discord state)
RECONCILIATION_AUTO_FIX=false
RECONCILIATION_PAUSED=false
RECONCILIATION_TIMEZONE=America/New_York
RECONCILIATION_HOUR=3

# Admin email for reconciliation reports (optional)
ADMIN_EMAIL=admin@yourdomain.com
```

**Fix naming inconsistency:**
- Change `DISCORD_ADMIN_ALERT_CHANNEL_ID` to `DISCORD_ADMIN_CHANNEL_ID` (matches env.ts)

**Reorganize into clear sections:**
1. Server Configuration (NODE_ENV, PORT, APP_URL)
2. Database (DATABASE_URL, DIRECT_URL)
3. JWT / Session (JWT_SECRET)
4. Stripe Keys (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET)
5. Stripe Price IDs (STRIPE_INDIVIDUAL_PRICE_ID, etc.)
6. Discord OAuth (DISCORD_CLIENT_ID, DISCORD_CLIENT_SECRET, etc.)
7. Discord Channels (DISCORD_INTRODUCTIONS_CHANNEL_ID, etc.)
8. Email Configuration (EMAIL_PROVIDER, RESEND_API_KEY, EMAIL_FROM_ADDRESS, EMAIL_REPLY_TO)
9. Reconciliation (RECONCILIATION_*, ADMIN_EMAIL)
10. Admin Seed (ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD)

Each section should have:
- Clear comment header with section name
- Source instructions for each variable (e.g., "Get from: Stripe Dashboard -> Developers -> API keys")
- Example values where helpful (e.g., sk_test_...)
  </action>
  <verify>
    - grep -c "RECONCILIATION_AUTO_FIX" .env.example returns 1
    - grep -c "EMAIL_FROM_ADDRESS" .env.example returns 1
    - grep -c "ADMIN_EMAIL" .env.example returns 1
    - grep -c "DISCORD_ADMIN_CHANNEL_ID" .env.example returns 1
    - grep -c "DISCORD_ADMIN_ALERT_CHANNEL_ID" .env.example returns 0 (removed)
  </verify>
  <done>.env.example contains all 37 environment variables from env.ts with clear section organization</done>
</task>

</tasks>

<verification>
- [ ] README.md exists in project root
- [ ] README.md contains Quick Start with 5 commands
- [ ] README.md links to ENVIRONMENT-SETUP.md
- [ ] README.md links to docs/API.md
- [ ] .env.example contains RECONCILIATION_AUTO_FIX
- [ ] .env.example contains EMAIL_FROM_ADDRESS
- [ ] .env.example uses DISCORD_ADMIN_CHANNEL_ID (not ALERT variant)
- [ ] Total variable count in .env.example >= 30
</verification>

<success_criteria>
- New developer reading README.md understands project purpose and can start development
- .env.example serves as complete template - no hunting through env.ts required
</success_criteria>

<output>
After completion, create `.planning/phases/21-documentation-audit/21-01-SUMMARY.md`
</output>
