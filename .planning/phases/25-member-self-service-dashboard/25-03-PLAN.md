# 25-03: Member Dashboard Frontend with Navigation, Account, and Billing Pages

---
phase: 25-member-self-service-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - public/dashboard.html
  - public/account.html
  - public/billing.html
autonomous: true

must_haves:
  truths:
    - "Dashboard has navigation header with Dashboard, Account, Billing links"
    - "User can change email from Account page"
    - "User can change password from Account page"
    - "Billing page shows current plan, payment method (last 4), next billing date"
    - "Billing page shows invoice history with PDF links"
    - "Dashboard shows activity timeline"
    - "Team owners see link to team dashboard"
    - "Team members see team info"
    - "Token auto-refresh prevents session expiry during active use"
  artifacts:
    - path: "public/dashboard.html"
      provides: "Dashboard with navigation and activity timeline"
      contains: "member-nav"
    - path: "public/account.html"
      provides: "Account settings page with email/password forms"
      contains: "update-email"
    - path: "public/billing.html"
      provides: "Billing details page with invoices"
      contains: "billing-details"
  key_links:
    - from: "public/dashboard.html"
      to: "/dashboard"
      via: "fetch for member data"
      pattern: "fetch.*dashboard"
    - from: "public/account.html"
      to: "/auth/update-email"
      via: "fetch for email update"
      pattern: "fetch.*update-email"
    - from: "public/billing.html"
      to: "/billing/details"
      via: "fetch for billing data"
      pattern: "fetch.*billing/details"
---

<objective>
Create the complete member self-service dashboard frontend with navigation header, account settings page, and billing details page.

Purpose: Transform the minimal status page into a full self-managed membership portal per Phase 25 success criteria 1-8.

Output:
- Enhanced dashboard.html with navigation header, activity timeline, team info
- New account.html with email and password change forms
- New billing.html with subscription info, payment method, invoice history
- Token auto-refresh on all authenticated pages
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-member-self-service-dashboard/25-RESEARCH.md
@.planning/phases/25-member-self-service-dashboard/25-01-SUMMARY.md (after Plan 01 completes)

Key patterns from existing codebase:
- Admin dashboard nav structure (public/admin/dashboard.html lines 22-29)
- Medieval theme with Cinzel/Crimson Text fonts, gold (#d4af37) on dark (#1a1a2e)
- localStorage for accessToken, fetch with Authorization: Bearer header
- /app/* routes for authenticated pages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance dashboard.html with navigation and token auto-refresh</name>
  <files>public/dashboard.html</files>
  <action>
Update the existing dashboard.html:

1. Add navigation header after the opening body tag, before dashboard-header:
```html
<nav class="member-nav">
  <div class="nav-brand">The Revenue Council</div>
  <div class="nav-links">
    <a href="/app/dashboard" class="nav-link active">Dashboard</a>
    <a href="/app/account" class="nav-link">Account</a>
    <a href="/app/billing" class="nav-link">Billing</a>
  </div>
  <button class="nav-logout" onclick="logout()">Logout</button>
</nav>
```

2. Add navigation CSS in the style section (before dashboard-container styles):
```css
.member-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 2rem;
  background: var(--bg-card);
  border-bottom: 1px solid var(--border-subtle);
}

.nav-brand {
  font-family: 'Cinzel', serif;
  font-size: 1.25rem;
  color: var(--gold);
  font-weight: 700;
}

.nav-links {
  display: flex;
  gap: 2rem;
}

.nav-link {
  font-family: 'Cinzel', serif;
  color: var(--cream);
  text-decoration: none;
  padding: 0.5rem 0;
  border-bottom: 2px solid transparent;
  transition: all 0.3s ease;
}

.nav-link:hover {
  color: var(--gold);
}

.nav-link.active {
  color: var(--gold);
  border-bottom-color: var(--gold);
}

.nav-logout {
  background: transparent;
  border: 1px solid var(--gold);
  color: var(--gold);
  padding: 0.5rem 1rem;
  font-family: 'Cinzel', serif;
  cursor: pointer;
  border-radius: 4px;
  transition: all 0.3s ease;
}

.nav-logout:hover {
  background: var(--gold);
  color: var(--bg-dark);
}

@media (max-width: 768px) {
  .member-nav {
    flex-wrap: wrap;
    gap: 1rem;
  }
  .nav-links {
    order: 3;
    width: 100%;
    justify-content: center;
    gap: 1rem;
  }
}
```

3. Add activity timeline section after the discord-section div:
```html
<!-- Activity Timeline -->
<div class="timeline-section" id="timeline-section" style="display: none;">
  <h3>Activity Timeline</h3>
  <ul class="timeline-list" id="timeline-list">
    <!-- Populated by JavaScript -->
  </ul>
</div>
```

4. Add timeline CSS:
```css
.timeline-section {
  background: var(--bg-section);
  border: 1px solid var(--border-subtle);
  border-radius: 12px;
  padding: 2rem;
  margin: 2rem 0;
}

.timeline-section h3 {
  font-size: 1.25rem;
  margin-bottom: 1rem;
}

.timeline-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.timeline-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  padding: 1rem 0;
  border-bottom: 1px solid var(--border-subtle);
}

.timeline-item:last-child {
  border-bottom: none;
}

.timeline-icon {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
  flex-shrink: 0;
}

.timeline-icon.joined { background: rgba(96, 165, 250, 0.2); color: #60a5fa; }
.timeline-icon.subscribed { background: rgba(74, 222, 128, 0.2); color: #4ade80; }
.timeline-icon.discord_claimed { background: rgba(139, 92, 246, 0.2); color: #8b5cf6; }
.timeline-icon.introduced { background: rgba(212, 175, 55, 0.2); color: var(--gold); }

.timeline-content {
  flex: 1;
}

.timeline-content p {
  margin: 0 0 0.25rem 0;
  color: var(--cream);
}

.timeline-date {
  font-size: 0.85rem;
  color: var(--text-muted);
}
```

5. Add team dashboard link in action buttons section (update renderActionButtons):
After the Discord invite button check, add:
```javascript
// Team dashboard link for owners
if (data.team && data.team.isOwner) {
  buttons.push('<a href="/app/team-dashboard" class="action-button">Team Dashboard</a>');
}

// Team info for team members
if (data.team && !data.team.isOwner) {
  // Show team name badge (already in status card)
}
```

6. Add renderTimeline function:
```javascript
function renderTimeline(timeline) {
  if (!timeline || timeline.length === 0) return;

  const timelineSection = document.getElementById('timeline-section');
  const timelineList = document.getElementById('timeline-list');

  const icons = {
    joined: '&#9733;',      // star
    subscribed: '&#10003;', // checkmark
    discord_claimed: '&#9819;', // chess piece
    introduced: '&#9829;'   // heart
  };

  timelineList.innerHTML = timeline.map(item => `
    <li class="timeline-item">
      <div class="timeline-icon ${item.type}">${icons[item.type] || '&#8226;'}</div>
      <div class="timeline-content">
        <p>${escapeHtml(item.description)}</p>
        <span class="timeline-date">${formatDate(item.date)}</span>
      </div>
    </li>
  `).join('');

  timelineSection.style.display = 'block';
}
```

7. Update renderDashboard to call renderTimeline:
```javascript
// In renderDashboard function, after renderActionButtons:
renderTimeline(data.timeline);
```

8. Add token auto-refresh functionality (add before loadDashboard function):
```javascript
const TOKEN_REFRESH_INTERVAL = 10 * 60 * 1000; // 10 minutes
let refreshIntervalId = null;

function parseJwt(token) {
  try {
    const base64Url = token.split('.')[1];
    const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
    return JSON.parse(atob(base64));
  } catch (e) {
    return null;
  }
}

function isTokenExpiringSoon(token) {
  const payload = parseJwt(token);
  if (!payload || !payload.exp) return true;
  const expiresIn = payload.exp * 1000 - Date.now();
  return expiresIn < 2 * 60 * 1000; // Less than 2 minutes
}

async function refreshTokenIfNeeded() {
  const token = getAccessToken();
  if (!token || !isTokenExpiringSoon(token)) return;

  try {
    const response = await fetch('/auth/refresh', {
      method: 'POST',
      credentials: 'include',
    });
    if (response.ok) {
      const { accessToken } = await response.json();
      localStorage.setItem('accessToken', accessToken);
    }
  } catch (err) {
    console.warn('Token refresh failed:', err);
  }
}

function setupTokenAutoRefresh() {
  refreshTokenIfNeeded();
  refreshIntervalId = setInterval(refreshTokenIfNeeded, TOKEN_REFRESH_INTERVAL);
}
```

9. Update DOMContentLoaded to setup auto-refresh:
```javascript
document.addEventListener('DOMContentLoaded', () => {
  setupTokenAutoRefresh();
  loadDashboard();
});
```

10. Update renderSubscriptionStatus to show team name for team members:
In the seat tier section, if data.team exists and member is not owner:
```javascript
if (member.seatTier && member.subscriptionStatus === 'ACTIVE') {
  let tierDisplay = getTierDisplayName(member.seatTier);
  // Show team name for team members
  if (data.team && member.seatTier === 'TEAM_MEMBER') {
    tierDisplay += ' at ' + escapeHtml(data.team.name);
  }
  seatTier.innerHTML = `Rank: <span class="tier-badge">${tierDisplay}</span>`;
}
```
Note: renderSubscriptionStatus needs access to data.team, so pass it: renderSubscriptionStatus(data.member, data.team);

11. Remove the duplicate logout button from action buttons (it's now in the nav).
  </action>
  <verify>
Open /app/dashboard in browser:
1. Navigation header displays with Dashboard (active), Account, Billing links
2. Activity timeline shows member events (joined, subscribed, etc.)
3. Team owners see "Team Dashboard" button
4. Token auto-refreshes in background (check Network tab after 10+ min)
5. Responsive layout works on mobile
  </verify>
  <done>
Dashboard has navigation header, activity timeline, team dashboard link for owners, team info for members, and token auto-refresh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create account.html for email and password management</name>
  <files>public/account.html</files>
  <action>
Create new file public/account.html with account settings forms:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Account Settings - The Revenue Council</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Crimson+Text:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
  <style>
    /* Navigation styles (same as dashboard) */
    .member-nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem 2rem;
      background: var(--bg-card);
      border-bottom: 1px solid var(--border-subtle);
    }

    .nav-brand {
      font-family: 'Cinzel', serif;
      font-size: 1.25rem;
      color: var(--gold);
      font-weight: 700;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
    }

    .nav-link {
      font-family: 'Cinzel', serif;
      color: var(--cream);
      text-decoration: none;
      padding: 0.5rem 0;
      border-bottom: 2px solid transparent;
      transition: all 0.3s ease;
    }

    .nav-link:hover {
      color: var(--gold);
    }

    .nav-link.active {
      color: var(--gold);
      border-bottom-color: var(--gold);
    }

    .nav-logout {
      background: transparent;
      border: 1px solid var(--gold);
      color: var(--gold);
      padding: 0.5rem 1rem;
      font-family: 'Cinzel', serif;
      cursor: pointer;
      border-radius: 4px;
      transition: all 0.3s ease;
    }

    .nav-logout:hover {
      background: var(--gold);
      color: var(--bg-dark);
    }

    /* Account page styles */
    .account-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
    }

    .page-header {
      text-align: center;
      padding: 2rem;
    }

    .page-header h1 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .settings-card {
      background: var(--bg-card);
      border: 1px solid var(--border-subtle);
      border-radius: 12px;
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .settings-card h2 {
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
      color: var(--gold);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-label {
      display: block;
      margin-bottom: 0.5rem;
      color: var(--cream);
      font-family: 'Cinzel', serif;
    }

    .form-input {
      width: 100%;
      padding: 0.75rem 1rem;
      background: var(--bg-dark);
      border: 1px solid var(--border-subtle);
      border-radius: 4px;
      color: var(--cream);
      font-family: 'Crimson Text', serif;
      font-size: 1rem;
    }

    .form-input:focus {
      outline: none;
      border-color: var(--gold);
    }

    .form-button {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      background: var(--gold);
      border: none;
      color: var(--bg-dark);
      font-family: 'Cinzel', serif;
      font-size: 0.9rem;
      border-radius: 4px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .form-button:hover {
      background: var(--gold-light);
    }

    .form-button:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .current-email {
      padding: 0.75rem 1rem;
      background: var(--bg-section);
      border: 1px solid var(--border-subtle);
      border-radius: 4px;
      color: var(--cream);
      margin-bottom: 1rem;
    }

    .success-message {
      padding: 1rem;
      background: rgba(74, 222, 128, 0.1);
      border: 1px solid rgba(74, 222, 128, 0.3);
      border-radius: 4px;
      color: #4ade80;
      margin-bottom: 1rem;
      display: none;
    }

    .success-message.visible {
      display: block;
    }

    .error-message {
      padding: 1rem;
      background: rgba(239, 68, 68, 0.1);
      border: 1px solid rgba(239, 68, 68, 0.3);
      border-radius: 4px;
      color: #ef4444;
      margin-bottom: 1rem;
      display: none;
    }

    .error-message.visible {
      display: block;
    }

    .loading {
      text-align: center;
      padding: 4rem 2rem;
      color: var(--text-muted);
    }

    .loading-spinner {
      width: 40px;
      height: 40px;
      border: 3px solid var(--border-subtle);
      border-top-color: var(--gold);
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin: 0 auto 1rem;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    @media (max-width: 768px) {
      .member-nav {
        flex-wrap: wrap;
        gap: 1rem;
      }
      .nav-links {
        order: 3;
        width: 100%;
        justify-content: center;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>
  <nav class="member-nav">
    <div class="nav-brand">The Revenue Council</div>
    <div class="nav-links">
      <a href="/app/dashboard" class="nav-link">Dashboard</a>
      <a href="/app/account" class="nav-link active">Account</a>
      <a href="/app/billing" class="nav-link">Billing</a>
    </div>
    <button class="nav-logout" onclick="logout()">Logout</button>
  </nav>

  <header class="page-header">
    <h1>Account Settings</h1>
    <p id="user-email" style="color: var(--text-muted);">Loading...</p>
  </header>

  <main class="account-container">
    <div id="loading" class="loading">
      <div class="loading-spinner"></div>
      <p>Loading your account...</p>
    </div>

    <div id="account-content" style="display: none;">
      <!-- Change Email -->
      <div class="settings-card">
        <h2>Change Email Address</h2>
        <div id="email-success" class="success-message"></div>
        <div id="email-error" class="error-message"></div>
        <form id="email-form">
          <div class="form-group">
            <label class="form-label">Current Email</label>
            <div class="current-email" id="current-email">-</div>
          </div>
          <div class="form-group">
            <label for="new-email" class="form-label">New Email Address</label>
            <input type="email" id="new-email" class="form-input" placeholder="new@example.com" required>
          </div>
          <div class="form-group">
            <label for="email-password" class="form-label">Current Password</label>
            <input type="password" id="email-password" class="form-input" placeholder="Confirm your password" required>
          </div>
          <button type="submit" class="form-button" id="email-btn">Update Email</button>
        </form>
      </div>

      <!-- Change Password -->
      <div class="settings-card">
        <h2>Change Password</h2>
        <div id="password-success" class="success-message"></div>
        <div id="password-error" class="error-message"></div>
        <form id="password-form">
          <div class="form-group">
            <label for="current-password" class="form-label">Current Password</label>
            <input type="password" id="current-password" class="form-input" placeholder="Your current password" required>
          </div>
          <div class="form-group">
            <label for="new-password" class="form-label">New Password</label>
            <input type="password" id="new-password" class="form-input" placeholder="At least 8 characters" required minlength="8">
          </div>
          <div class="form-group">
            <label for="confirm-password" class="form-label">Confirm New Password</label>
            <input type="password" id="confirm-password" class="form-input" placeholder="Confirm new password" required>
          </div>
          <button type="submit" class="form-button" id="password-btn">Update Password</button>
        </form>
      </div>
    </div>
  </main>

  <footer>
    <div class="footer-content">
      <p class="copyright">&copy; The Revenue Council. All rights reserved.</p>
    </div>
  </footer>

  <script>
    // Token management
    function getAccessToken() {
      return localStorage.getItem('accessToken');
    }

    // Token auto-refresh
    const TOKEN_REFRESH_INTERVAL = 10 * 60 * 1000;

    function parseJwt(token) {
      try {
        const base64Url = token.split('.')[1];
        const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
        return JSON.parse(atob(base64));
      } catch (e) {
        return null;
      }
    }

    function isTokenExpiringSoon(token) {
      const payload = parseJwt(token);
      if (!payload || !payload.exp) return true;
      return (payload.exp * 1000 - Date.now()) < 2 * 60 * 1000;
    }

    async function refreshTokenIfNeeded() {
      const token = getAccessToken();
      if (!token || !isTokenExpiringSoon(token)) return;
      try {
        const response = await fetch('/auth/refresh', {
          method: 'POST',
          credentials: 'include',
        });
        if (response.ok) {
          const { accessToken } = await response.json();
          localStorage.setItem('accessToken', accessToken);
        }
      } catch (err) {
        console.warn('Token refresh failed:', err);
      }
    }

    function setupTokenAutoRefresh() {
      refreshTokenIfNeeded();
      setInterval(refreshTokenIfNeeded, TOKEN_REFRESH_INTERVAL);
    }

    // Logout
    function logout() {
      localStorage.removeItem('accessToken');
      window.location.href = '/';
    }

    // Load account data
    async function loadAccount() {
      const token = getAccessToken();
      if (!token) {
        window.location.href = '/app/auth/login?redirect=/app/account';
        return;
      }

      try {
        const response = await fetch('/dashboard', {
          headers: { 'Authorization': `Bearer ${token}` },
        });

        if (response.status === 401) {
          localStorage.removeItem('accessToken');
          window.location.href = '/app/auth/login?redirect=/app/account';
          return;
        }

        const data = await response.json();
        document.getElementById('user-email').textContent = data.member.email;
        document.getElementById('current-email').textContent = data.member.email;
        document.getElementById('loading').style.display = 'none';
        document.getElementById('account-content').style.display = 'block';
      } catch (err) {
        document.getElementById('loading').innerHTML = '<p style="color: #ef4444;">Failed to load account</p>';
      }
    }

    // Email form submission
    document.getElementById('email-form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const successDiv = document.getElementById('email-success');
      const errorDiv = document.getElementById('email-error');
      const btn = document.getElementById('email-btn');

      successDiv.classList.remove('visible');
      errorDiv.classList.remove('visible');
      btn.disabled = true;
      btn.textContent = 'Updating...';

      const newEmail = document.getElementById('new-email').value;
      const currentPassword = document.getElementById('email-password').value;

      try {
        const response = await fetch('/auth/update-email', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${getAccessToken()}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ newEmail, currentPassword }),
        });

        const data = await response.json();

        if (!response.ok) {
          errorDiv.textContent = data.error || 'Failed to update email';
          errorDiv.classList.add('visible');
        } else {
          successDiv.textContent = 'Email updated successfully!';
          successDiv.classList.add('visible');
          document.getElementById('current-email').textContent = data.email;
          document.getElementById('user-email').textContent = data.email;
          document.getElementById('new-email').value = '';
          document.getElementById('email-password').value = '';
        }
      } catch (err) {
        errorDiv.textContent = 'Failed to connect to server';
        errorDiv.classList.add('visible');
      }

      btn.disabled = false;
      btn.textContent = 'Update Email';
    });

    // Password form submission
    document.getElementById('password-form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const successDiv = document.getElementById('password-success');
      const errorDiv = document.getElementById('password-error');
      const btn = document.getElementById('password-btn');

      successDiv.classList.remove('visible');
      errorDiv.classList.remove('visible');

      const currentPassword = document.getElementById('current-password').value;
      const newPassword = document.getElementById('new-password').value;
      const confirmPassword = document.getElementById('confirm-password').value;

      if (newPassword !== confirmPassword) {
        errorDiv.textContent = 'New passwords do not match';
        errorDiv.classList.add('visible');
        return;
      }

      btn.disabled = true;
      btn.textContent = 'Updating...';

      try {
        const response = await fetch('/auth/update-password', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${getAccessToken()}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ currentPassword, newPassword }),
        });

        const data = await response.json();

        if (!response.ok) {
          errorDiv.textContent = data.error || 'Failed to update password';
          errorDiv.classList.add('visible');
        } else {
          successDiv.textContent = 'Password updated successfully!';
          successDiv.classList.add('visible');
          document.getElementById('current-password').value = '';
          document.getElementById('new-password').value = '';
          document.getElementById('confirm-password').value = '';
        }
      } catch (err) {
        errorDiv.textContent = 'Failed to connect to server';
        errorDiv.classList.add('visible');
      }

      btn.disabled = false;
      btn.textContent = 'Update Password';
    });

    // Initialize
    document.addEventListener('DOMContentLoaded', () => {
      setupTokenAutoRefresh();
      loadAccount();
    });
  </script>
</body>
</html>
```
  </action>
  <verify>
Open /app/account in browser:
1. Navigation shows Account as active
2. Current email displays correctly
3. Email update form submits and shows success/error
4. Password update form validates matching passwords
5. Password update submits and shows success/error
  </verify>
  <done>
Account page allows users to change email (with password confirmation) and change password (with current password confirmation).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create billing.html for subscription and invoice display</name>
  <files>public/billing.html</files>
  <action>
Create new file public/billing.html with billing information display:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Billing - The Revenue Council</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Crimson+Text:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
  <style>
    /* Navigation styles */
    .member-nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem 2rem;
      background: var(--bg-card);
      border-bottom: 1px solid var(--border-subtle);
    }

    .nav-brand {
      font-family: 'Cinzel', serif;
      font-size: 1.25rem;
      color: var(--gold);
      font-weight: 700;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
    }

    .nav-link {
      font-family: 'Cinzel', serif;
      color: var(--cream);
      text-decoration: none;
      padding: 0.5rem 0;
      border-bottom: 2px solid transparent;
      transition: all 0.3s ease;
    }

    .nav-link:hover {
      color: var(--gold);
    }

    .nav-link.active {
      color: var(--gold);
      border-bottom-color: var(--gold);
    }

    .nav-logout {
      background: transparent;
      border: 1px solid var(--gold);
      color: var(--gold);
      padding: 0.5rem 1rem;
      font-family: 'Cinzel', serif;
      cursor: pointer;
      border-radius: 4px;
      transition: all 0.3s ease;
    }

    .nav-logout:hover {
      background: var(--gold);
      color: var(--bg-dark);
    }

    /* Billing page styles */
    .billing-container {
      max-width: 800px;
      margin: 0 auto;
      padding: 2rem;
    }

    .page-header {
      text-align: center;
      padding: 2rem;
    }

    .page-header h1 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .billing-card {
      background: var(--bg-card);
      border: 1px solid var(--border-subtle);
      border-radius: 12px;
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .billing-card h2 {
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
      color: var(--gold);
    }

    .billing-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 0;
      border-bottom: 1px solid var(--border-subtle);
    }

    .billing-row:last-child {
      border-bottom: none;
    }

    .billing-label {
      color: var(--text-muted);
      font-family: 'Cinzel', serif;
    }

    .billing-value {
      color: var(--cream);
      font-weight: 600;
    }

    .billing-value.highlight {
      color: var(--gold);
    }

    .status-badge {
      display: inline-block;
      padding: 0.35rem 0.75rem;
      border-radius: 4px;
      font-family: 'Cinzel', serif;
      font-size: 0.85rem;
      text-transform: uppercase;
    }

    .status-badge.active {
      background: rgba(74, 222, 128, 0.2);
      color: #4ade80;
    }

    .status-badge.past_due {
      background: rgba(251, 191, 36, 0.2);
      color: #fbbf24;
    }

    .status-badge.canceled {
      background: rgba(239, 68, 68, 0.2);
      color: #ef4444;
    }

    .card-display {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .card-icon {
      font-size: 1.5rem;
    }

    .card-details {
      display: flex;
      flex-direction: column;
    }

    .card-brand {
      text-transform: capitalize;
      color: var(--cream);
    }

    .card-number {
      color: var(--text-muted);
      font-size: 0.9rem;
    }

    .action-button {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      background: var(--gold);
      border: none;
      color: var(--bg-dark);
      font-family: 'Cinzel', serif;
      font-size: 0.9rem;
      border-radius: 4px;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .action-button:hover {
      background: var(--gold-light);
    }

    /* Invoice table */
    .invoice-table {
      width: 100%;
      border-collapse: collapse;
    }

    .invoice-table th,
    .invoice-table td {
      padding: 1rem;
      text-align: left;
      border-bottom: 1px solid var(--border-subtle);
    }

    .invoice-table th {
      color: var(--text-muted);
      font-family: 'Cinzel', serif;
      font-weight: normal;
      font-size: 0.9rem;
    }

    .invoice-table td {
      color: var(--cream);
    }

    .invoice-link {
      color: var(--gold);
      text-decoration: none;
    }

    .invoice-link:hover {
      text-decoration: underline;
    }

    .invoice-status {
      padding: 0.25rem 0.5rem;
      border-radius: 3px;
      font-size: 0.8rem;
      text-transform: capitalize;
    }

    .invoice-status.paid {
      background: rgba(74, 222, 128, 0.2);
      color: #4ade80;
    }

    .invoice-status.open {
      background: rgba(251, 191, 36, 0.2);
      color: #fbbf24;
    }

    .invoice-status.void {
      background: rgba(156, 163, 175, 0.2);
      color: #9ca3af;
    }

    .empty-state {
      text-align: center;
      padding: 2rem;
      color: var(--text-muted);
    }

    .managed-by-team {
      text-align: center;
      padding: 3rem;
    }

    .managed-by-team h2 {
      color: var(--gold);
      margin-bottom: 1rem;
    }

    .managed-by-team p {
      color: var(--cream);
      margin-bottom: 0.5rem;
    }

    .loading {
      text-align: center;
      padding: 4rem 2rem;
      color: var(--text-muted);
    }

    .loading-spinner {
      width: 40px;
      height: 40px;
      border: 3px solid var(--border-subtle);
      border-top-color: var(--gold);
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin: 0 auto 1rem;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    @media (max-width: 768px) {
      .member-nav {
        flex-wrap: wrap;
        gap: 1rem;
      }
      .nav-links {
        order: 3;
        width: 100%;
        justify-content: center;
        gap: 1rem;
      }
      .invoice-table {
        font-size: 0.9rem;
      }
      .invoice-table th,
      .invoice-table td {
        padding: 0.75rem 0.5rem;
      }
    }
  </style>
</head>
<body>
  <nav class="member-nav">
    <div class="nav-brand">The Revenue Council</div>
    <div class="nav-links">
      <a href="/app/dashboard" class="nav-link">Dashboard</a>
      <a href="/app/account" class="nav-link">Account</a>
      <a href="/app/billing" class="nav-link active">Billing</a>
    </div>
    <button class="nav-logout" onclick="logout()">Logout</button>
  </nav>

  <header class="page-header">
    <h1>Billing & Payments</h1>
  </header>

  <main class="billing-container">
    <div id="loading" class="loading">
      <div class="loading-spinner"></div>
      <p>Loading billing details...</p>
    </div>

    <div id="billing-content" style="display: none;">
      <!-- Team member view -->
      <div id="managed-by-team" class="billing-card managed-by-team" style="display: none;">
        <h2>Billing Managed by Team</h2>
        <p>Your membership is part of <strong id="team-name">-</strong></p>
        <p style="color: var(--text-muted);">Contact your team administrator for billing inquiries.</p>
      </div>

      <!-- Owner/Individual view -->
      <div id="billing-details" style="display: none;">
        <!-- Subscription Info -->
        <div class="billing-card">
          <h2>Current Subscription</h2>
          <div class="billing-row">
            <span class="billing-label">Plan</span>
            <span class="billing-value" id="plan-name">-</span>
          </div>
          <div class="billing-row">
            <span class="billing-label">Status</span>
            <span id="subscription-status">-</span>
          </div>
          <div class="billing-row">
            <span class="billing-label">Next Billing Date</span>
            <span class="billing-value" id="next-billing">-</span>
          </div>
          <div class="billing-row" id="cancel-row" style="display: none;">
            <span class="billing-label">Cancels At</span>
            <span class="billing-value" style="color: #ef4444;" id="cancels-at">-</span>
          </div>
        </div>

        <!-- Payment Method -->
        <div class="billing-card">
          <h2>Payment Method</h2>
          <div id="payment-method-display">
            <div class="billing-row">
              <div class="card-display">
                <span class="card-icon" id="card-icon">&#128179;</span>
                <div class="card-details">
                  <span class="card-brand" id="card-brand">-</span>
                  <span class="card-number" id="card-number">-</span>
                </div>
              </div>
              <span class="billing-value" id="card-expiry">-</span>
            </div>
          </div>
          <div id="no-payment-method" style="display: none;">
            <p class="empty-state">No payment method on file</p>
          </div>
          <div style="margin-top: 1.5rem;">
            <button class="action-button" onclick="openBillingPortal()">Manage Payment Method</button>
          </div>
        </div>

        <!-- Invoice History -->
        <div class="billing-card">
          <h2>Invoice History</h2>
          <div id="invoices-container">
            <table class="invoice-table" id="invoice-table" style="display: none;">
              <thead>
                <tr>
                  <th>Date</th>
                  <th>Amount</th>
                  <th>Status</th>
                  <th>Invoice</th>
                </tr>
              </thead>
              <tbody id="invoice-list">
              </tbody>
            </table>
            <div id="no-invoices" class="empty-state" style="display: none;">
              No invoices yet
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <div class="footer-content">
      <p class="copyright">&copy; The Revenue Council. All rights reserved.</p>
    </div>
  </footer>

  <script>
    // Token management
    function getAccessToken() {
      return localStorage.getItem('accessToken');
    }

    // Token auto-refresh
    const TOKEN_REFRESH_INTERVAL = 10 * 60 * 1000;

    function parseJwt(token) {
      try {
        const base64Url = token.split('.')[1];
        const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
        return JSON.parse(atob(base64));
      } catch (e) {
        return null;
      }
    }

    function isTokenExpiringSoon(token) {
      const payload = parseJwt(token);
      if (!payload || !payload.exp) return true;
      return (payload.exp * 1000 - Date.now()) < 2 * 60 * 1000;
    }

    async function refreshTokenIfNeeded() {
      const token = getAccessToken();
      if (!token || !isTokenExpiringSoon(token)) return;
      try {
        const response = await fetch('/auth/refresh', {
          method: 'POST',
          credentials: 'include',
        });
        if (response.ok) {
          const { accessToken } = await response.json();
          localStorage.setItem('accessToken', accessToken);
        }
      } catch (err) {
        console.warn('Token refresh failed:', err);
      }
    }

    function setupTokenAutoRefresh() {
      refreshTokenIfNeeded();
      setInterval(refreshTokenIfNeeded, TOKEN_REFRESH_INTERVAL);
    }

    // Logout
    function logout() {
      localStorage.removeItem('accessToken');
      window.location.href = '/';
    }

    // Format date
    function formatDate(dateString) {
      if (!dateString) return '-';
      return new Date(dateString).toLocaleDateString('en-US', {
        year: 'numeric',
        month: 'long',
        day: 'numeric',
      });
    }

    // Format currency
    function formatCurrency(amount) {
      return '$' + amount.toFixed(2);
    }

    // Open billing portal
    async function openBillingPortal() {
      try {
        const response = await fetch('/billing/portal', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${getAccessToken()}`,
            'Content-Type': 'application/json',
          },
        });

        if (!response.ok) {
          const error = await response.json();
          alert(error.error || 'Failed to open billing portal');
          return;
        }

        const { portalUrl } = await response.json();
        window.location.href = portalUrl;
      } catch (error) {
        alert('Failed to open billing portal. Please try again.');
      }
    }

    // Load billing details
    async function loadBilling() {
      const token = getAccessToken();
      if (!token) {
        window.location.href = '/app/auth/login?redirect=/app/billing';
        return;
      }

      try {
        const response = await fetch('/billing/details', {
          headers: { 'Authorization': `Bearer ${token}` },
        });

        if (response.status === 401) {
          localStorage.removeItem('accessToken');
          window.location.href = '/app/auth/login?redirect=/app/billing';
          return;
        }

        const data = await response.json();
        renderBilling(data);
      } catch (err) {
        document.getElementById('loading').innerHTML = '<p style="color: #ef4444;">Failed to load billing details</p>';
      }
    }

    // Render billing information
    function renderBilling(data) {
      document.getElementById('loading').style.display = 'none';
      document.getElementById('billing-content').style.display = 'block';

      // Team member view
      if (data.managedBy === 'team') {
        document.getElementById('managed-by-team').style.display = 'block';
        document.getElementById('team-name').textContent = data.teamName || 'your team';
        return;
      }

      // Owner/Individual view
      document.getElementById('billing-details').style.display = 'block';

      // Subscription info
      if (data.subscription) {
        document.getElementById('plan-name').textContent = data.subscription.planName || 'Subscription';

        const statusSpan = document.getElementById('subscription-status');
        const status = data.subscription.status;
        statusSpan.innerHTML = `<span class="status-badge ${status}">${status.replace('_', ' ')}</span>`;

        document.getElementById('next-billing').textContent = formatDate(data.subscription.currentPeriodEnd);

        if (data.subscription.cancelAtPeriodEnd) {
          document.getElementById('cancel-row').style.display = 'flex';
          document.getElementById('cancels-at').textContent = formatDate(data.subscription.currentPeriodEnd);
        }
      } else {
        document.getElementById('plan-name').textContent = 'No active subscription';
        document.getElementById('subscription-status').innerHTML = '<span class="status-badge" style="background: rgba(156, 163, 175, 0.2); color: #9ca3af;">None</span>';
        document.getElementById('next-billing').textContent = '-';
      }

      // Payment method
      if (data.paymentMethod) {
        document.getElementById('payment-method-display').style.display = 'block';
        document.getElementById('no-payment-method').style.display = 'none';

        const brand = data.paymentMethod.brand;
        document.getElementById('card-brand').textContent = brand;
        document.getElementById('card-number').textContent = '**** **** **** ' + data.paymentMethod.last4;
        document.getElementById('card-expiry').textContent = `Expires ${data.paymentMethod.expMonth}/${data.paymentMethod.expYear}`;

        // Set card icon based on brand
        const brandIcons = {
          visa: '&#128179;',
          mastercard: '&#128179;',
          amex: '&#128179;',
          default: '&#128179;'
        };
        document.getElementById('card-icon').innerHTML = brandIcons[brand.toLowerCase()] || brandIcons.default;
      } else {
        document.getElementById('payment-method-display').style.display = 'none';
        document.getElementById('no-payment-method').style.display = 'block';
      }

      // Invoices
      if (data.invoices && data.invoices.length > 0) {
        document.getElementById('invoice-table').style.display = 'table';
        document.getElementById('no-invoices').style.display = 'none';

        const invoiceList = document.getElementById('invoice-list');
        invoiceList.innerHTML = data.invoices.map(inv => `
          <tr>
            <td>${formatDate(inv.date)}</td>
            <td>${formatCurrency(inv.amount)}</td>
            <td><span class="invoice-status ${inv.status}">${inv.status}</span></td>
            <td>
              ${inv.hostedUrl ? `<a href="${inv.hostedUrl}" target="_blank" class="invoice-link">View</a>` : ''}
              ${inv.pdfUrl ? ` | <a href="${inv.pdfUrl}" target="_blank" class="invoice-link">PDF</a>` : ''}
            </td>
          </tr>
        `).join('');
      } else {
        document.getElementById('invoice-table').style.display = 'none';
        document.getElementById('no-invoices').style.display = 'block';
      }
    }

    // Initialize
    document.addEventListener('DOMContentLoaded', () => {
      setupTokenAutoRefresh();
      loadBilling();
    });
  </script>
</body>
</html>
```
  </action>
  <verify>
Open /app/billing in browser:
1. Navigation shows Billing as active
2. For individual/owner: shows subscription status, payment method (last 4), next billing date
3. Shows invoice history with View/PDF links
4. "Manage Payment Method" button opens Stripe billing portal
5. For team members: shows "Billing Managed by Team" message
  </verify>
  <done>
Billing page displays current plan, payment method (brand and last 4 digits), next billing date, and invoice history for owners/individuals. Team members see managed-by-team message.
  </done>
</task>

</tasks>

<verification>
```bash
# Verify all HTML files exist and have no syntax errors
ls -la public/dashboard.html public/account.html public/billing.html

# Check for required elements
grep -l "member-nav" public/dashboard.html public/account.html public/billing.html
grep -l "refreshTokenIfNeeded" public/dashboard.html public/account.html public/billing.html
grep -l "timeline" public/dashboard.html

# Start server and manually test in browser
npm run dev
# Visit /app/dashboard, /app/account, /app/billing
```
</verification>

<success_criteria>
- Dashboard has navigation header with Dashboard (active), Account, Billing links
- Dashboard shows activity timeline with member events
- Dashboard shows team dashboard link for owners, team info for members
- Account page allows email change with password confirmation
- Account page allows password change with current password verification
- Billing page shows subscription status, plan name, next billing date
- Billing page shows payment method with brand and last 4 digits
- Billing page shows invoice history with View/PDF links
- Billing page shows "managed by team" for team members
- Token auto-refresh runs on all three pages
- All pages responsive on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/25-member-self-service-dashboard/25-03-SUMMARY.md`
</output>
